#include <main.h>
/**
 *message is putchar
 */
int main(void)
{
   int str[]={95, 112, 117, 99, 104, 97, 114};
   int count, sz;
   sz = sizeof(str)/ sizeof(int);
   for (count=o; count<sz; count++)
   {
      _putchar(str[count]);
   }
   _putchar('\n');
   return (0);
}
#include <_putchar.h>
/**
 *message is putchar
 */
int main(void)
{
   int str[]={95, 112, 117, 99, 104, 97, 114};
   int count, sz;
   sz = sizeof(str)/ sizeof(int);
   for (count=o; count<sz; count++)
   {
      _putchar(str[count]);
   }
   _putchar('\n');
   return (0);
}
